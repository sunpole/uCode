# uCode

Python desktop utilities for producing EAN-13 barcodes and print-ready PDF output. The primary application is `barcode_gui.py`.

It requires the Python packages used by the source: `python-barcode`, `reportlab`, and `Pillow`.
